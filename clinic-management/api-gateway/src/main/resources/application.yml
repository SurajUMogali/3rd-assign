server:
  port: 8194

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3307/clinicdb
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: api-gateway

